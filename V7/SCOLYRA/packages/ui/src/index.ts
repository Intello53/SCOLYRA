/**
 * @scolyra/ui — réservé pour une future bibliothèque de composants
 * partagés entre apps/web et d'éventuelles futures apps (mobile,
 * admin séparé...).
 *
 * Pour l'instant, TOUS les composants réels de SCOLYRA vivent dans
 * apps/web/components/ (Panel, Button, Sidebar, etc.) — ce package
 * existe pour que le monorepo ait la structure prévue dès le départ
 * (§6 de l'architecture), mais n'est pas encore utilisé. Si un jour
 * un composant doit être partagé au-delà de apps/web, c'est ici qu'il
 * devrait être déplacé.
 */
export const UI_PACKAGE_PLACEHOLDER = true;
